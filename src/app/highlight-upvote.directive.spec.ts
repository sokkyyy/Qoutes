import { HighlightUpvoteDirective } from './highlight-upvote.directive';

describe('HighlightUpvoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightUpvoteDirective();
    expect(directive).toBeTruthy();
  });
});
